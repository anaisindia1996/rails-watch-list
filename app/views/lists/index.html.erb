<h1>List of Lists</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list %></li>
    <%= cl_image_tag list.photo.key, height: 300, width: 400, crop: :fill %>

  <% end %>
</ul>


<p><%= link_to "Add a new list", new_list_path %></p>
